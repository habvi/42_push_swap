#ifndef SOLVE_H
# define SOLVE_H

# include <stdbool.h>
// # include <unistd.h>

// typedef struct s_deque	t_deque;
// typedef enum e_error	t_error;

// solve.c
void	solve(t_nums *nums, int *sorted_array);

#endif

