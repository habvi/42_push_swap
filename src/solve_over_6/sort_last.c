#include "deque.h"
#include "error.h"
#include "push_swap.h"
#include "solve.h"

#include <stdio.h>

t_nums	*sort_from_large_num(t_deque *block, t_data *data, t_error *error_code)
{
	(void)block;
	(void)error_code;
	return (data->now_op);
}
