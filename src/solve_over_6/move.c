#include "deque.h"
#include "error.h"
#include "push_swap.h"
#include "solve.h"

#include <stdio.h> // to do: erase

t_nums	*move_for_divide_nums(t_block *block, t_data *data, t_error *error_code)
{
	(void)block;
	(void)error_code;
	return (data->now_op);
}
